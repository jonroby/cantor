import * as lib from '@/lib';

export const renameWithDedup = lib.renameWithDedup;
export const diffLines = lib.diffLines;
export const mapDocument = lib.mapDocument;
export const validate = lib.validate;
export const marked = lib.marked;
