import * as lib from '@/lib';

export const renameWithDedup = lib.rename.renameWithDedup;
export const diffLines = lib.diff.diffLines;
export const mapDocument = lib.documentMap.mapDocument;
export const validate = lib.validateMd.validate;
export const marked = lib.documentMap.marked;
