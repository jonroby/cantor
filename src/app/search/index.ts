import * as domain from '@/domain';

export const getDefaultItems = domain.search.getDefaultItems;
export const searchChats = domain.search.searchChats;

export type SearchResult = domain.search.SearchResult;
