# Lib Review Agent

Read:

1. `rules/root.md`
2. `rules/lib.md`

Review changes in `src/lib/**` against those rules.

Prioritize:

- non-generic logic in lib
- frontend rendering helpers creeping back in
- one-off exports that should stay local
- unclear naming for shared helpers
