# External Review Agent

Read:

1. `rules/root.md`
2. `rules/external.md`

Review changes in `src/external/**` against those rules.

Prioritize:

- app semantics inside boundary code
- state-shaped return types
- naming policy inside external
- runtime-state coupling that should be explicit inputs
